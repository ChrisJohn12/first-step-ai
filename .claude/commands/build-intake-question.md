Create a new intake question component for the
First Step AI onboarding flow based on: $ARGUMENTS

Create or update these files:
1. /src/components/questions/[QuestionName].jsx
   - React component with question text
   - Answer options as buttons
   - Passes selected answer up to parent via onChange prop
   - Tailwind styling, mobile first

2. /src/data/questions.js
   - Add this question to the questions array
   - Include question id, text, options, and next question logic

Follow existing component patterns in /src/components/questions/
if they exist.
```

This skill touches multiple files at once — exactly what the tutorial is teaching — and it builds your actual intake flow component by component.

---

**Your session goal:**

By the end of this tutorial session you should have:

- `/new-pattern` working with `$ARGUMENTS`
- `/build-intake-question` skill created
- At least 3 real pattern files inside `src/data/patterns/` generated by running `/new-pattern`
- The beginning of your `src/data/questions.js` file

Test it by running in Claude Code:
```
/new-pattern shipping cost optimization for Etsy seller doing 50 orders per month who overpays on postage